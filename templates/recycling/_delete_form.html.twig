<form method="post" action="{{ path('recycling_delete', {'id': recycling.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recycling.id) }}">
    <button class="btn">Delete</button>
</form>
