{% extends 'base.html.twig' %}

{% block title %}Recycling{% endblock %}

{% block body %}
    <h1>Recycling</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recycling.id }}</td>
            </tr>
            <tr>
                <th>RecyclingDate</th>
                <td>{{ recycling.recyclingDate ? recycling.recyclingDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recycling_index') }}">back to list</a>

    <a href="{{ path('recycling_edit', {'id': recycling.id}) }}">edit</a>

    {{ include('recycling/_delete_form.html.twig') }}
{% endblock %}
