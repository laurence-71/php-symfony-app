{% extends 'base.html.twig' %}

{% block title %}Edit Requirement{% endblock %}

{% block body %}
<div class="container-fluid list">
{% form_theme formRequirement 'bootstrap_4_layout.html.twig' %}
    <h1>Edit Requirement</h1>

    {{ include('requirement/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn create" href="{{ path('requirement_index') }}"><i class="bi bi-chevron-left"> Back to list</i></a>

    {{ include('requirement/_delete_form.html.twig') }}
    </div>
{% endblock %}
