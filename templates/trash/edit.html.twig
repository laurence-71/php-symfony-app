{% extends 'base.html.twig' %}

{% block title %}Edit Trash{% endblock %}

{% block body %}
<div class="container-fluid list">
{% form_theme formTrash 'bootstrap_4_layout.html.twig' %}
    <h1>Edit Trash</h1>

    {{ include('trash/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn create" href="{{ path('trash_index') }}"><i class="bi bi-chevron-left"> Back to list</i></a>

    {{ include('trash/_delete_form.html.twig') }}
    </div>
{% endblock %}
